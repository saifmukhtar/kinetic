import{_ as t,o as i,c as a,a2 as s}from"./chunks/framework.Nk14N-ai.js";const u=JSON.parse('{"title":"Future Horizons","description":"","frontmatter":{},"headers":[],"relativePath":"future_horizons.md","filePath":"future_horizons.md"}'),n={name:"future_horizons.md"};function o(r,e,l,h,c,d){return i(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="future-horizons" tabindex="-1">Future Horizons <a class="header-anchor" href="#future-horizons" aria-label="Permalink to &quot;Future Horizons&quot;">​</a></h1><p>The Kinetic Protocol, in its current v1 implementation, achieves the primary goal of creating a zero-cost, un-squattable, and fully forkable sovereign namespace engine. However, the naming layer is only the foundation.</p><p>This chapter details the architectural roadmap for Kinetic&#39;s future evolution — across four dimensions: the fork ecosystem, TLS certificate sovereignty, decentralized content hosting, and anonymous routing.</p><hr><h2 id="_1-the-fork-ecosystem" tabindex="-1">1. The Fork Ecosystem <a class="header-anchor" href="#_1-the-fork-ecosystem" aria-label="Permalink to &quot;1. The Fork Ecosystem&quot;">​</a></h2><p>The most significant near-term opportunity for Kinetic is not the <code>.kin</code> network itself — it is the ecosystem of organizations that fork and deploy it.</p><p><strong>Planned developments:</strong></p><ul><li><strong><code>kinetic-forge</code> GUI:</strong> A desktop application wrapping the interactive <code>kinetic-forge</code> wizard, allowing non-Rust developers to configure and deploy a fork with zero CLI knowledge</li><li><strong>Fork Registry:</strong> A voluntary public directory of active Kinetic forks — their TLDs, bootstrap nodes, and governance contacts — enabling cross-fork peering and discovery</li><li><strong>Cross-Network Resolution:</strong> A proposed extension allowing a <code>.kin</code> daemon to optionally resolve names on registered fork networks, creating a federated namespace without sacrificing individual network sovereignty</li><li><strong>Delegated Compute (Mobile):</strong> Completing the Nostr NIP-04 integration so mobile users can delegate VDF computation to their desktop daemon or a paid compute provider, enabling name registration from battery-constrained devices</li></ul><hr><h2 id="_2-the-certificate-authority-ca-bottleneck" tabindex="-1">2. The Certificate Authority (CA) Bottleneck <a class="header-anchor" href="#_2-the-certificate-authority-ca-bottleneck" aria-label="Permalink to &quot;2. The Certificate Authority (CA) Bottleneck&quot;">​</a></h2><p>Currently, the web relies heavily on HTTPS (TLS encryption). When your browser connects to <code>https://bank.com</code>, it receives a digital certificate guaranteeing that the server it is talking to is actually <code>bank.com</code>.</p><p>Who issues these certificates? A centralized oligopoly of Certificate Authorities (CAs) like Let&#39;s Encrypt, DigiCert, and Sectigo. These CAs are trusted by the major browser vendors (Google, Apple, Mozilla).</p><p>If you own <code>example.kin</code>, you cannot easily get an HTTPS certificate for it because standard CAs rely on ICANN-controlled DNS to verify domain ownership. Even if you could, relying on a centralized CA to issue a certificate for a decentralized domain defeats the philosophical purpose of Kinetic. A government could simply order a CA to revoke your certificate, instantly rendering your site untrusted by browsers.</p><h3 id="_1-1-the-solution-dane-and-self-signed-tls" tabindex="-1">1.1 The Solution: DANE and self-signed TLS <a class="header-anchor" href="#_1-1-the-solution-dane-and-self-signed-tls" aria-label="Permalink to &quot;1.1 The Solution: DANE and self-signed TLS&quot;">​</a></h3><p>To bypass the CA oligopoly, Kinetic will integrate <strong>DANE (DNS-Based Authentication of Named Entities)</strong> natively into the protocol.</p><p>DANE, defined in RFC 6698, allows domain owners to publish the exact cryptographic hash of their TLS certificate directly into the DNS system via a <code>TLSA</code> record.</p><p>In a future update to the <code>kinetic-core</code> types, the <code>Reveal</code> struct will be expanded to support arbitrary payload types beyond just IP addresses:</p><div class="language-rust vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pub</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> enum</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PayloadType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    IPv4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Ipv4Addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    IPv6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Ipv6Addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    TLSA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Vec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">u8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;), </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The SHA-256 hash of the self-signed TLS cert</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    TXT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">String</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h4 id="the-execution-flow" tabindex="-1">The Execution Flow: <a class="header-anchor" href="#the-execution-flow" aria-label="Permalink to &quot;The Execution Flow:&quot;">​</a></h4><ol><li>The domain owner generates their own self-signed TLS certificate locally on their server. No centralized CA is contacted.</li><li>The owner computes the SHA-256 hash of this certificate.</li><li>The owner executes the VDF grind to register <code>example.kin</code>, embedding both the IP address and the TLSA hash into the <code>Reveal</code> payload.</li><li>When a user navigates to <code>https://example.kin</code>, the local <code>kinetic-dns</code> daemon intercepts the request.</li><li>The daemon fetches the Kademlia payload, extracts the IP and the TLSA hash, and returns them to the browser.</li><li>The browser connects to the server, receives the self-signed certificate, and verifies that its hash perfectly matches the TLSA record retrieved securely from the Kademlia swarm.</li></ol><p>Because the Kademlia payload is secured by the user&#39;s Ed25519 signature and the VDF Proof of Patience, the TLSA record is completely unforgeable. The browser can confidently display the &quot;Secure Padlock&quot; icon without needing to trust DigiCert, Let&#39;s Encrypt, or any human authority.</p><hr><h2 id="_3-the-physical-hosting-bottleneck" tabindex="-1">3. The Physical Hosting Bottleneck <a class="header-anchor" href="#_3-the-physical-hosting-bottleneck" aria-label="Permalink to &quot;3. The Physical Hosting Bottleneck&quot;">​</a></h2><p>Mapping a decentralized name to a centralized IP address is a useful first step, but an IP address still points to a physical server sitting in a physical data center. That server can be unplugged by a hosting provider (like AWS or DigitalOcean), subpoenaed, or hit with a massive DDoS attack.</p><p>For Kinetic to achieve ultimate resilience, the domains must resolve to decentralized content networks rather than physical IP addresses.</p><h3 id="_2-1-content-addressing-via-ipfs" tabindex="-1">2.1 Content Addressing via IPFS <a class="header-anchor" href="#_2-1-content-addressing-via-ipfs" aria-label="Permalink to &quot;2.1 Content Addressing via IPFS&quot;">​</a></h3><p>The InterPlanetary File System (IPFS) changes the paradigm from &quot;Where is the data?&quot; (Location-based addressing, IPs) to &quot;What is the data?&quot; (Content-based addressing, CIDs).</p><p>In the future, the <code>kinetic</code> will allow users to bind an IPFS CID to their domain instead of an IP address.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cargo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> register</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> example.kin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> QmYwAPJzv5CZsnA625s3Xf2nemtYgPpHdWEz79ojWnPbdG</span></span></code></pre></div><p>When a user requests <code>example.kin</code>, the <code>kinetic-dns</code> daemon will query the Kademlia DHT, retrieve the <code>TXT</code> record containing the CID, and automatically redirect the traffic to a local IPFS gateway node.</p><p>The website files (HTML, CSS, JS) are served entirely from the P2P swarm. If the original author goes offline or is censored, the website remains perfectly accessible as long as at least one node on earth is pinning the CID.</p><p>Combining Kinetic with IPFS creates an unstoppable stack: An un-censorable naming system resolving to an un-censorable file system.</p><h3 id="_2-2-anonymity-via-tor-onion-services" tabindex="-1">2.2 Anonymity via Tor Onion Services <a class="header-anchor" href="#_2-2-anonymity-via-tor-onion-services" aria-label="Permalink to &quot;2.2 Anonymity via Tor Onion Services&quot;">​</a></h3><p>For users operating under extreme authoritarian regimes, simply serving content is not enough; the server and the visitor must remain physically anonymous.</p><p>Tor Onion Services (<code>.onion</code> addresses) provide flawless anonymity, but the addresses are horrific 56-character base32 strings (e.g., <code>expyuz5tatcgrvmxq...onion</code>), making them impossible to memorize or share verbally.</p><p>Kinetic can bridge this gap by acting as a human-readable alias for Tor.</p><p>The <code>Reveal</code> payload can easily store an Onion address. When the user requests <code>example.kin</code>, the daemon fetches the Kademlia payload and proxies the TCP connection directly into the local Tor routing daemon.</p><p>The user types a memorable, 7-character name into their standard browser, and instantly connects to a heavily encrypted, multi-hop anonymous service, completely abstracting the complex cryptography away from the user experience.</p><hr><h2 id="_4-kinetic-s-ultimate-goal" tabindex="-1">4. Kinetic&#39;s Ultimate Goal <a class="header-anchor" href="#_4-kinetic-s-ultimate-goal" aria-label="Permalink to &quot;4. Kinetic&#39;s Ultimate Goal&quot;">​</a></h2><p>The history of the internet is a cycle of decentralization followed by rapid corporate and governmental capture. The open protocols of the 1990s (HTTP, SMTP, DNS) were steadily enclosed by massive central authorities and monopolies.</p><p>Kinetic is not just a routing tool; it is a mathematical wedge designed to pry the base layer of the internet back open.</p><p>By replacing ICANN&#39;s political bureaucracy with VDF mathematics, replacing DNS registries with Kademlia DHTs, replacing CAs with DANE, and replacing physical servers with IPFS swarms, Kinetic aims to build a parallel web architecture where censorship is not just illegal, but physically and mathematically impossible.</p>`,43)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
