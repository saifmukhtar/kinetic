import{_ as t,o as a,c as i,a2 as o}from"./chunks/framework.Nk14N-ai.js";const p=JSON.parse('{"title":"Chapter 9: Exhaustive Code Walkthrough (kinetic-client & FFI)","description":"","frontmatter":{},"headers":[],"relativePath":"code_walkthrough_client.md","filePath":"code_walkthrough_client.md"}'),r={name:"code_walkthrough_client.md"};function n(s,e,l,c,d,h){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="chapter-9-exhaustive-code-walkthrough-kinetic-client-ffi" tabindex="-1">Chapter 9: Exhaustive Code Walkthrough (<code>kinetic-client</code> &amp; FFI) <a class="header-anchor" href="#chapter-9-exhaustive-code-walkthrough-kinetic-client-ffi" aria-label="Permalink to &quot;Chapter 9: Exhaustive Code Walkthrough (`kinetic-client` &amp; FFI)&quot;">​</a></h1><p>The previous chapters detailed the desktop and server infrastructure of Kinetic. However, mobile devices represent the vast majority of modern internet usage. Running a full Kademlia DHT node and computing massive VDF proofs on a smartphone would instantly drain the battery and violate OS background execution policies.</p><p>To solve this, Kinetic provides <code>kinetic-client</code>, a cross-platform mobile application built in Flutter (Dart) that interfaces with the Rust core via Foreign Function Interface (FFI). It resides in a separate repository (<code>/home/saif/kinetic-client</code>), rather than as a standard workspace crate.</p><p>In this chapter, we dissect how the mobile client acts as a lightweight, secure gateway to the Kinetic network without melting your phone.</p><hr><h2 id="_1-the-architectural-boundary-flutter-rust-bridge" tabindex="-1">1. The Architectural Boundary: <code>flutter_rust_bridge</code> <a class="header-anchor" href="#_1-the-architectural-boundary-flutter-rust-bridge" aria-label="Permalink to &quot;1. The Architectural Boundary: `flutter_rust_bridge`&quot;">​</a></h2><p>The mobile application is split into two strict domains:</p><ol><li><strong>The UI Thread (Dart):</strong> Handles animations, user interactions, OS permissions, and native Flutter screens for the Kinetic dashboard.</li><li><strong>The Core Engine (Rust):</strong> Handles cryptographic signing, DHT resolution, and payload validation via the <code>kinetic-ffi</code> crate.</li></ol><p>These two worlds communicate using <code>flutter_rust_bridge</code> (FRB). FRB automatically generates safe C-bindings and Dart wrappers for Rust functions.</p><h3 id="the-rust-ffi-api-kinetic-ffi" tabindex="-1">The Rust FFI API (<code>kinetic-ffi</code>) <a class="header-anchor" href="#the-rust-ffi-api-kinetic-ffi" aria-label="Permalink to &quot;The Rust FFI API (`kinetic-ffi`)&quot;">​</a></h3><p>The Rust side exposes high-level asynchronous functions. Unlike the Desktop daemon, the mobile client runs as a light client. The entry points are separated into specialized modules within <code>kinetic-ffi/src/api/</code>:</p><ul><li><strong><code>daemon.rs</code> (<code>init_light_client()</code>)</strong>: Initializes the lightweight Kademlia node that queries the DHT but does not store data for other peers.</li><li><strong><code>resolver.rs</code></strong>: Handles intercepts for the mobile WebView, ensuring <code>kin://</code> URLs are securely resolved and routed.</li><li><strong><code>delegation.rs</code></strong>: Manages the complex Nostr/HTTP flow required to outsource VDF computation to desktop nodes.</li></ul><h3 id="the-dart-ui-invocation" tabindex="-1">The Dart UI Invocation <a class="header-anchor" href="#the-dart-ui-invocation" aria-label="Permalink to &quot;The Dart UI Invocation&quot;">​</a></h3><p>The mobile app has native Flutter screens for the UI and only uses the WebView for resolving and viewing external <code>kin://</code> domains.</p><hr><h2 id="_2-dynamic-axum-proxies-webview-interception" tabindex="-1">2. Dynamic Axum Proxies &amp; WebView Interception <a class="header-anchor" href="#_2-dynamic-axum-proxies-webview-interception" aria-label="Permalink to &quot;2. Dynamic Axum Proxies &amp; WebView Interception&quot;">​</a></h2><p>A major challenge for mobile is securely rendering <code>kin://</code> domains within a standard Flutter WebView.</p><p>Instead of running a single fixed server on port <code>16001</code>, the mobile client uses <strong>dynamic Axum proxies</strong>. When the user navigates to a <code>kin://</code> domain, the FFI layer intercepts the request.</p><p>It calls <code>get_or_spawn_transport_bridge()</code> to spin up a temporary, dynamically allocated Axum server bound to a random localhost port specifically for that domain.</p><ul><li><strong>LRU Eviction</strong>: To prevent memory leaks and port exhaustion, the bridge manager uses LRU eviction, maintaining a maximum of 10 active bridges at a time.</li><li>The Flutter WebView is then seamlessly redirected to this temporary localhost port, allowing the user to browse the decentralized site exactly as if it were a standard HTTPS website.</li></ul><hr><h2 id="_3-vdf-delegation-via-nostr-http" tabindex="-1">3. VDF Delegation via Nostr &amp; HTTP <a class="header-anchor" href="#_3-vdf-delegation-via-nostr-http" aria-label="Permalink to &quot;3. VDF Delegation via Nostr &amp; HTTP&quot;">​</a></h2><p>The most computationally expensive operation in Kinetic is domain registration. Because a smartphone cannot physically compute a 4-million iteration VDF without overheating, it delegates the math to a Desktop node.</p><h3 id="the-delegation-flow" tabindex="-1">The Delegation Flow <a class="header-anchor" href="#the-delegation-flow" aria-label="Permalink to &quot;The Delegation Flow&quot;">​</a></h3><ol><li><strong>Mobile Request (<code>delegation.rs</code>)</strong>: The Rust engine generates a new Ed25519 identity, fetches the Drand pulse, creates the <code>CommitRequest</code>, and generates a small Hashcash Proof-of-Work to deter spam.</li><li><strong>Encrypted Transport</strong>: The request is encrypted and published either via a Nostr Relay (<code>wss://relay.kinetic.network</code>) or a direct HTTP fallback.</li><li><strong>Desktop Grind</strong>: The Desktop node verifies the PoW, computes the massive VDF proof, and returns the encrypted <code>Reveal</code> bytes to the phone.</li><li><strong>Finalization</strong>: The mobile phone signs the final payload with its locally secured private key and broadcasts it to the DHT.</li></ol><p>The private key never leaves the phone, ensuring maximum security while outsourcing the unfeasible math.</p><hr><h2 id="_4-background-heartbeats-hardware-attestation" tabindex="-1">4. Background Heartbeats &amp; Hardware Attestation <a class="header-anchor" href="#_4-background-heartbeats-hardware-attestation" aria-label="Permalink to &quot;4. Background Heartbeats &amp; Hardware Attestation&quot;">​</a></h2><p>Kinetic names require periodic &quot;heartbeats&quot; (rebroadcasting the Reveal) to remain active.</p><ul><li><strong>Background Heartbeat Task</strong>: The mobile app utilizes the Flutter <code>workmanager</code> plugin to wake up periodically in the background. It retrieves the encrypted Ed25519 keys securely stored via <code>FlutterSecureStorage</code>, signs the heartbeat, and broadcasts it to the DHT without requiring the user to open the app.</li><li><strong>Hardware Attestation</strong>: To combat massive botnets registering names from emulators, the mobile client integrates Hardware Attestation (Google Play Integrity API for Android and DeviceCheck for iOS). This ensures that delegation requests are originating from real, uncompromised physical devices.</li></ul>',30)])])}const g=t(r,[["render",n]]);export{p as __pageData,g as default};
