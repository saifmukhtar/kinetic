import{_ as t,o as a,c as i,a2 as o}from"./chunks/framework.Nk14N-ai.js";const m=JSON.parse('{"title":"Chapter 3: P2P Routing and The Immunological DHT","description":"","frontmatter":{},"headers":[],"relativePath":"network_architecture.md","filePath":"network_architecture.md"}'),s={name:"network_architecture.md"};function n(r,e,l,c,d,h){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="chapter-3-p2p-routing-and-the-immunological-dht" tabindex="-1">Chapter 3: P2P Routing and The Immunological DHT <a class="header-anchor" href="#chapter-3-p2p-routing-and-the-immunological-dht" aria-label="Permalink to &quot;Chapter 3: P2P Routing and The Immunological DHT&quot;">​</a></h1><p>While the cryptography described in Chapter 2 establishes the rules of the Kinetic Protocol, the network layer is the physical battleground where those rules are enforced. Kinetic operates without a central server, blockchain, or master database. Instead, it utilizes a <strong>Distributed Hash Table (DHT)</strong> to route and store data across a fluid, constantly shifting swarm of peer-to-peer nodes.</p><p>This chapter details how Kinetic adapts the standard Kademlia routing algorithm into a hostile, mathematically rigorous &quot;Immunological DHT&quot; capable of defending itself against spam, Eclipse attacks, and Sybil swarms.</p><hr><h2 id="_1-the-basics-of-kademlia-routing" tabindex="-1">1. The Basics of Kademlia Routing <a class="header-anchor" href="#_1-the-basics-of-kademlia-routing" aria-label="Permalink to &quot;1. The Basics of Kademlia Routing&quot;">​</a></h2><p>Kinetic&#39;s networking stack is built on <code>libp2p</code>, specifically utilizing the <strong>Kademlia DHT</strong> protocol.</p><p>In a traditional client-server model, locating data is trivial: you ask the central server. In a completely decentralized network consisting of thousands of anonymous laptops and servers globally, finding the specific node that holds the IP address for <code>apple.kin</code> is a complex computer science problem.</p><p>Kademlia solves this by treating both nodes and data as points in a massive, mathematically uniform space (typically a 256-bit keyspace).</p><h3 id="the-xor-distance-metric" tabindex="-1">The XOR Distance Metric <a class="header-anchor" href="#the-xor-distance-metric" aria-label="Permalink to &quot;The XOR Distance Metric&quot;">​</a></h3><p>When you start a Kinetic Daemon, it generates a unique cryptographic identity (a <code>PeerId</code>). When you register a name like <code>apple.kin</code>, that name is hashed into a 256-bit key (\\(K = H(\\text{&quot;apple.kin&quot;}) \\)).</p><p>Kademlia routes data by calculating the &quot;distance&quot; between two keys using the exclusive OR (XOR) bitwise operation:</p><p>\\[ \\text{Distance}(A, B) = A \\oplus B \\]</p><p>This XOR metric is brilliant because it is perfectly symmetric (\\(A \\oplus B = B \\oplus A\\)), unidirectional, and satisfies the triangle inequality.</p><p>When your node wants to find the payload for <code>apple.kin</code>, it asks its closest known peers, &quot;Do you know who is closer to \\(K\\)?&quot; Those peers respond with nodes they know that are mathematically closer to \\(K\\). This process repeats iteratively. Because of the XOR topology, the search space halves with every hop. It guarantees that any node or piece of data in the network can be found in exactly \\(O(\\log N)\\) network hops, regardless of how massive the network scales.</p><hr><h2 id="_2-why-standard-kademlia-fails-for-naming-systems" tabindex="-1">2. Why Standard Kademlia Fails for Naming Systems <a class="header-anchor" href="#_2-why-standard-kademlia-fails-for-naming-systems" aria-label="Permalink to &quot;2. Why Standard Kademlia Fails for Naming Systems&quot;">​</a></h2><p>Kademlia is a routing marvel, but it was designed as a &quot;blind&quot; bulletin board.</p><p>In standard Kademlia, if Node A sends a <code>PUT</code> request containing 2 kilobytes of arbitrary data to Node B, Node B simply stores it. Kademlia assumes a relatively friendly network.</p><p>If we deployed the Kinetic Protocol on standard Kademlia, it would instantly collapse. A single malicious attacker could open thousands of connections and spam millions of fake <code>Reveal</code> payloads filled with garbage data. The DHT nodes would blindly accept the data, instantly exhausting their hard drives and bandwidth, resulting in a catastrophic Denial of Service (DoS).</p><p>To survive in an adversarial environment, Kinetic implements an <strong>Immunological DHT</strong>.</p><hr><h2 id="_3-the-immunological-dht-competitive-gossip" tabindex="-1">3. The Immunological DHT: Competitive Gossip <a class="header-anchor" href="#_3-the-immunological-dht-competitive-gossip" aria-label="Permalink to &quot;3. The Immunological DHT: Competitive Gossip&quot;">​</a></h2><p>Kinetic fundamentally alters Kademlia by decoupling <em>data availability</em> from <em>state validation</em>, moving consensus to the routing layer itself.</p><p>Inside <code>kinetic-network</code>, the standard <code>libp2p</code> Kademlia implementation is augmented with a highly customized <code>KineticRecordStore</code>. This store acts as a cryptographic firewall.</p><h3 id="active-mathematical-filtering" tabindex="-1">Active Mathematical Filtering <a class="header-anchor" href="#active-mathematical-filtering" aria-label="Permalink to &quot;Active Mathematical Filtering&quot;">​</a></h3><p>When a Kinetic node receives a <code>PUT</code> request for a domain, it does not blindly store it. Instead, before a single byte touches the disk or is propagated to other peers, the node executes the following deterministic validation loop:</p><ol><li><strong>Size Limits:</strong> Does the payload strictly adhere to the <strong>64 KB size limit</strong>? If not, it is instantly dropped to prevent OOM panics.</li><li><strong>Format Validation:</strong> Does the payload correctly deserialize into a <code>Reveal</code> or <code>CommitRequest</code> struct?</li><li><strong>Signature Verification:</strong> Is the Ed25519 signature valid against the embedded public key?</li><li><strong>VDF Mathematical Proof:</strong> Does the Chia VDF proof cleanly verify the commitment hash and the required number of iterations?</li></ol><p>If the payload fails <em>any</em> of these checks, the node instantly rejects the record.</p><p>This mechanism is called <strong>Competitive Gossip</strong>. The network acts as an active immune system. Cryptographically invalid data is destroyed upon contact. An attacker cannot flood the network with fake domain claims because honest nodes refuse to store them and refuse to gossip them to their neighbors. Only mathematically pristine data consumes storage space.</p><h3 id="lightweight-hashcash-proof-of-connection" tabindex="-1">Lightweight Hashcash Proof-of-Connection <a class="header-anchor" href="#lightweight-hashcash-proof-of-connection" aria-label="Permalink to &quot;Lightweight Hashcash Proof-of-Connection&quot;">​</a></h3><p>What if an attacker tries to execute a CPU exhaustion attack by sending millions of mathematically invalid VDFs to a single honest node, forcing the node to constantly verify (and reject) them?</p><p>While VDF verification is extremely fast (\\(O(\\log T)\\)), it still requires CPU cycles. To mitigate this, Kinetic nodes implement a lightweight <strong>Hashcash PoW</strong> at the connection layer.</p><p>When a peer connects, they are required to solve a trivial, 50-millisecond Hashcash puzzle before they are allowed to submit DHT requests. If a peer submits a mathematically invalid VDF, their &quot;reputation&quot; drops. If they submit multiple invalid VDFs, the honest node drops the TCP connection entirely and bans their IP address. To reconnect and resume the attack, the attacker must pay the Hashcash PoW again.</p><p>This makes sustained CPU exhaustion attacks economically irrational, as the attacker burns vastly more compute power generating the connections than the honest node burns verifying the VDFs.</p><hr><h2 id="_4-redundant-deterministic-storage-mitigating-eclipse-attacks" tabindex="-1">4. Redundant Deterministic Storage (Mitigating Eclipse Attacks) <a class="header-anchor" href="#_4-redundant-deterministic-storage-mitigating-eclipse-attacks" aria-label="Permalink to &quot;4. Redundant Deterministic Storage (Mitigating Eclipse Attacks)&quot;">​</a></h2><p>The final, most critical vulnerability in any DHT is the <strong>Eclipse Attack</strong>.</p><p>In standard Kademlia, data is stored at a single specific key \\(K = H(\\text{&quot;apple.kin&quot;})\\). If an adversary wants to censor <code>apple.kin</code>, they can generate thousands of Sybil nodes with <code>PeerIds</code> mathematically adjacent to \\(K\\). Eventually, the attacker&#39;s malicious nodes become the authoritative storage peers for \\(K\\).</p><p>When an honest user queries <code>apple.kin</code>, their request routes directly to the attacker&#39;s nodes. The attacker simply replies &quot;Record Not Found&quot; or serves an outdated payload. The honest payload is effectively &quot;eclipsed&quot; from the network.</p><p>Because Kinetic client-side validation can only verify data it receives, the client has no way to know the true payload was censored.</p><h3 id="multi-key-scattering" tabindex="-1">Multi-Key Scattering <a class="header-anchor" href="#multi-key-scattering" aria-label="Permalink to &quot;Multi-Key Scattering&quot;">​</a></h3><p>To definitively neutralize Eclipse attacks without resorting to centralized servers, Kinetic utilizes <strong>Redundant Deterministic Storage</strong>.</p><p>Instead of storing the payload at a single key \\(K\\), the registrant&#39;s daemon mathematically scatters the exact same signed payload across \\(M=32\\) independent, uncorrelated locations in the DHT.</p><p>The keys are derived using a domain-separated cryptographic hash: \\[ K_i = H(\\text{&quot;apple.kin&quot;} \\parallel i \\parallel \\text{&quot;kinetic-dht&quot;}), \\quad \\text{for } i \\in {0, 1, \\dots, M-1} \\]</p><p>Because the hash function acts as a random oracle, \\(K_0\\), \\(K_1\\), and \\(K_2\\) are located in completely different, uniformly random sectors of the global Kademlia keyspace.</p><p>When an honest user wants to resolve <code>apple.kin</code>, their client fires off parallel Kademlia <code>GET</code> queries to all locations simultaneously. It then takes the union of all returned payloads, validates their VDFs, and selects the genuine record.</p><h3 id="the-probabilistic-impossibility-of-eclipsing" tabindex="-1">The Probabilistic Impossibility of Eclipsing <a class="header-anchor" href="#the-probabilistic-impossibility-of-eclipsing" aria-label="Permalink to &quot;The Probabilistic Impossibility of Eclipsing&quot;">​</a></h3><p>Why is this so powerful?</p><p>Let&#39;s assume a highly capable attacker controls an astounding <strong>20%</strong> (\\(f = 0.2\\)) of all nodes in the global Kinetic network. The probability of the attacker successfully clustering enough Sybil nodes to eclipse a single key is roughly equal to \\(f\\).</p><p>With Kinetic Protocol V2 using \\(M = 32\\) redundant keys, the keys are mathematically independent. The probability that the attacker successfully eclipses all 32 keys simultaneously is:</p><p>\\[ P(\\text{total eclipse}) = f^M = (0.2)^{32} \\approx 4.29 \\times 10^{-23} \\]</p><p>By simply duplicating a small 64KB-limited payload across multiple independent keys, the probability of an Eclipse attack drops from a distinct threat to a statistical impossibility. The marginal bandwidth overhead for the user is low, but the censorship resistance is multiplied exponentially.</p><p>Through Competitive Gossip, Hashcash PoW, and Redundant Deterministic Storage, Kinetic transforms the naive Kademlia DHT into an impenetrable fortress of data availability.</p>',53)])])}const u=t(s,[["render",n]]);export{m as __pageData,u as default};
