import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.Nk14N-ai.js";const u=JSON.parse('{"title":"kinetic-storage: Sled Storage Wrapper","description":"","frontmatter":{},"headers":[],"relativePath":"kinetic_storage.md","filePath":"kinetic_storage.md"}'),i={name:"kinetic_storage.md"};function s(n,e,c,d,l,p){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="kinetic-storage-sled-storage-wrapper" tabindex="-1">kinetic-storage: Sled Storage Wrapper <a class="header-anchor" href="#kinetic-storage-sled-storage-wrapper" aria-label="Permalink to &quot;kinetic-storage: Sled Storage Wrapper&quot;">​</a></h1><p>The Kinetic protocol requires lightning-fast, persistent storage to maintain cryptographic proofs, configuration states, and active VDF reveals across reboots. <code>kinetic-storage</code> provides a robust wrapper around the <code>sled</code> embedded database.</p><h2 id="the-sled-database" tabindex="-1">The Sled Database <a class="header-anchor" href="#the-sled-database" aria-label="Permalink to &quot;The Sled Database&quot;">​</a></h2><p><code>sled</code> is a high-performance, embedded, thread-safe database written entirely in Rust. It functions similarly to SQLite but is optimized for massive concurrent throughput, making it ideal for the asynchronous demands of the <code>kinetic-daemon</code>.</p><h2 id="key-features-auto-recovery-logic" tabindex="-1">Key Features &amp; Auto-Recovery Logic <a class="header-anchor" href="#key-features-auto-recovery-logic" aria-label="Permalink to &quot;Key Features &amp; Auto-Recovery Logic&quot;">​</a></h2><p>Because <code>sled</code> relies on memory-mapped files and aggressive caching, sudden power losses or hard crashes can occasionally lead to file corruption. <code>kinetic-storage</code> implements advanced wrapper logic to handle these catastrophic events gracefully.</p><h3 id="auto-recovery-corrupt-bak-renaming" tabindex="-1">Auto-Recovery (<code>.corrupt.bak</code> Renaming) <a class="header-anchor" href="#auto-recovery-corrupt-bak-renaming" aria-label="Permalink to &quot;Auto-Recovery (`.corrupt.bak` Renaming)&quot;">​</a></h3><p>When <code>kinetic-storage</code> attempts to initialize the <code>sled</code> database, it explicitly catches corruption errors. Instead of panicking and crashing the daemon permanently, it executes an auto-recovery protocol:</p><ol><li><strong>Detection</strong>: It detects the specific <code>sled::Error</code> indicating database corruption.</li><li><strong>Quarantine</strong>: It renames the corrupted database directory by appending <code>.corrupt.bak</code> (e.g., <code>/tmp/kinetic_db.corrupt.bak</code>).</li><li><strong>Reinitialization</strong>: It creates a fresh, empty database directory in the original location and attempts to boot again.</li></ol><p>This ensures that the Kinetic daemon can self-heal from unrecoverable storage corruption without requiring manual user intervention. While local un-published state may be lost in this scenario, the daemon will successfully restart, allowing it to re-sync with the DHT and resume operations.</p><h3 id="edge-case-resiliency-tests" tabindex="-1">Edge Case Resiliency Tests <a class="header-anchor" href="#edge-case-resiliency-tests" aria-label="Permalink to &quot;Edge Case Resiliency Tests&quot;">​</a></h3><p>The storage engine is fortified by the <code>test_009_edge_cases.rs</code> suite. This testing framework specifically subjects the <code>sled</code> wrapper to intense concurrent read/write stress, sudden corruption simulations, and bloated payload injections to guarantee that the local state behaves predictably under extreme duress.</p>',12)])])}const g=t(i,[["render",s]]);export{u as __pageData,g as default};
