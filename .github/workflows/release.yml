name: Build and Release Binaries

on:
  release:
    types: [created]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build_linux_gnu:
    name: Build Linux GNU
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libgmp-dev clang libclang-dev
      - name: Build Workspace (Release)
        run: cargo build --release --workspace
      - name: Rename and Upload Artifacts
        run: |
          mkdir -p artifacts
          cp target/release/kinetic-daemon artifacts/kinetic-daemon-linux-gnu
          cp target/release/kinetic artifacts/kinetic-cli-linux-gnu
          cp target/release/kinetic-node artifacts/kinetic-node-linux-gnu
          cp target/release/kinetic-host artifacts/kinetic-host-linux-gnu
          cp target/release/kinetic-keygen artifacts/kinetic-keygen-linux-gnu
          cp target/release/kinetic-dns artifacts/kinetic-dns-server-linux-gnu
          cp target/release/kinetic-pac artifacts/kinetic-pac-linux-gnu
          if command -v sha256sum >/dev/null 2>&1; then
            cd artifacts && sha256sum * > checksums-linux-gnu.txt
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: linux-gnu-binaries
          path: artifacts/*

  build_linux_musl:
    name: Build Linux Musl (Alpine)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install musl C++ compiler
        run: |
          wget -q https://musl.cc/x86_64-linux-musl-cross.tgz
          tar xzf x86_64-linux-musl-cross.tgz
          echo "$PWD/x86_64-linux-musl-cross/bin" >> $GITHUB_PATH
          echo "CC=x86_64-linux-musl-gcc" >> $GITHUB_ENV
          echo "CXX=x86_64-linux-musl-g++" >> $GITHUB_ENV
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl
      - uses: Swatinem/rust-cache@v2
      - name: Build Workspace (Release)
        run: cargo build --release --workspace --target x86_64-unknown-linux-musl
      - name: Rename and Upload Artifacts
        run: |
          mkdir -p artifacts
          cp target/x86_64-unknown-linux-musl/release/kinetic-daemon artifacts/kinetic-daemon-linux-musl
          cp target/x86_64-unknown-linux-musl/release/kinetic artifacts/kinetic-cli-linux-musl
          cp target/x86_64-unknown-linux-musl/release/kinetic-node artifacts/kinetic-node-linux-musl
          cp target/x86_64-unknown-linux-musl/release/kinetic-host artifacts/kinetic-host-linux-musl
          cp target/x86_64-unknown-linux-musl/release/kinetic-keygen artifacts/kinetic-keygen-linux-musl
          cp target/x86_64-unknown-linux-musl/release/kinetic-dns artifacts/kinetic-dns-server-linux-musl
          cp target/x86_64-unknown-linux-musl/release/kinetic-pac artifacts/kinetic-pac-linux-musl
          if command -v sha256sum >/dev/null 2>&1; then
            cd artifacts && sha256sum * > checksums-linux-musl.txt
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: linux-musl-binaries
          path: artifacts/*

  build_macos:
    name: Build macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install dependencies
        run: brew install gmp
      - name: Build Workspace (Release)
        run: cargo build --release --workspace
      - name: Rename and Upload Artifacts
        run: |
          mkdir -p artifacts
          cp target/release/kinetic-daemon artifacts/kinetic-daemon-macos
          cp target/release/kinetic artifacts/kinetic-cli-macos
          cp target/release/kinetic-node artifacts/kinetic-node-macos
          cp target/release/kinetic-host artifacts/kinetic-host-macos
          cp target/release/kinetic-keygen artifacts/kinetic-keygen-macos
          cp target/release/kinetic-dns artifacts/kinetic-dns-server-macos
          cp target/release/kinetic-pac artifacts/kinetic-pac-macos
          if command -v sha256sum >/dev/null 2>&1; then
            cd artifacts && sha256sum * > checksums-macos.txt
          else
            cd artifacts && shasum -a 256 * > checksums-macos.txt
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: macos-binaries
          path: artifacts/*

  build_windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - name: Enable Longpaths
        run: git config --system core.longpaths true
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install MPIR
        run: |
          cd kinetic-vdf/chiavdf
          git clone https://github.com/Chia-Network/mpir_gc_x64.git
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build Workspace (Release)
        run: cargo build --release --workspace
      - name: Rename and Upload Artifacts
        shell: bash
        run: |
          mkdir -p artifacts
          cp target/release/kinetic-daemon.exe artifacts/kinetic-daemon-windows.exe
          cp target/release/kinetic.exe artifacts/kinetic-cli-windows.exe
          cp target/release/kinetic-node.exe artifacts/kinetic-node-windows.exe
          cp target/release/kinetic-host.exe artifacts/kinetic-host-windows.exe
          cp target/release/kinetic-keygen.exe artifacts/kinetic-keygen-windows.exe
          cp target/release/kinetic-dns.exe artifacts/kinetic-dns-server-windows.exe
          cp target/release/kinetic-pac.exe artifacts/kinetic-pac-windows.exe
          cd artifacts && sha256sum * > checksums-windows.txt
      - uses: actions/upload-artifact@v4
        with:
          name: windows-binaries
          path: artifacts/*

  publish_release:
    name: Publish GitHub Release
    needs: [build_linux_gnu, build_linux_musl, build_macos, build_windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          merge-multiple: true
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: release-artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
