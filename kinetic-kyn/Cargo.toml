[package]
name = "kinetic-kyn"
version = "0.2.0"
edition = "2024"

[dependencies]
serde = { workspace = true, features = ["derive"] }
drand-verify = { workspace = true }

[build-dependencies]
serde_json = { workspace = true }
