[package]
name = "kinetic-verify"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
description = "Lightweight cryptographic verification logic for the Kinetic Network."
keywords = ["kinetic", "verification", "ml-dsa", "crypto"]
categories = ["cryptography"]

[dependencies]
# --- Workspace & Internal Crates ---
kinetic-primitives = { path = "../kinetic-primitives" }
kinetic-types = { path = "../kinetic-types" }

# --- Workspace Inherited ---
thiserror.workspace = true

# --- External Crates ---
base64.workspace = true

[dev-dependencies]
# --- Workspace & Internal Crates ---
kinetic-kid = { path = "../kinetic-kid" }

# --- External Crates ---
proptest.workspace = true
rand.workspace = true
