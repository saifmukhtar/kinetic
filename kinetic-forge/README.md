# kinetic-forge

CLI wizard for bootstrapping and scaffolding isolated private Kinetic networks.

This tool helps initialize test environments, configure `network.json` files, and generate local keys.
